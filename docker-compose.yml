version:  '2'
services:
  dev:
    build: .
    command: bash -c 'python3 manage.py makemigrations &&
                      python3 manage.py migrate --noinput &&
                      uwsgi --ini uwsgi.ini'
    volumes:
      - .:/home/service
    ports:
      - '5901:8000'